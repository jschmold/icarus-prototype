#pragma once

namespace Icarus {
  struct Transform {
    // The location, what blocks are taken up
    int x;
    int y;
    // the width and height of the blocks taken
    int width;
    int height;
  };
}
